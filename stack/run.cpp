#include <iostream>
#include <stack>
#include <vector>
#include <cctype>
using namespace std;

/**
 * Given An Array of Integer Height respresenting the histogram's bar height where the width of each bar is 1, return the area of largest rectangle in the histogram.
 */

bool max_area_in_histogram(string str){
    stack<char> q;
    int n = str.length();


}


int main(){
 
}