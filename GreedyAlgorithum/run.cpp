#include <iostream>
#include <vector>
#include <algorithm>
#include <functional>
using namespace std;

/**
 * Chocola Problem
 * Given a chocolate bar of length N and an array of integers A of size N-1 where A[i] is the cost of breaking the chocolate bar between the i-th and (i+1)-th square. 
 * Find the minimum cost to break the chocolate bar into N pieces.
 * Input: N = 6, A = {2, 1, 3, 1, 4}
 * Output: 10
*/

void chocola_problem(vector<pair<int , int>> &job){
    int n = job.size();
 
}

int main(){
    vector<pair<int , int>> job_sch = {{4, 20}, {1, 10}, {1, 40}, {1, 30}};
    chocola_problem(job_sch);
}